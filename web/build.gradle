apply plugin: 'jetty'

dependencies {
    compile project(':core')

    testCompile 'junit:junit:4.10'
}

buildscript {
    repositories {
        mavenCentral()
    }
}

jar {
    baseName = 'user-management'
    version =  '0.1.0'
}

